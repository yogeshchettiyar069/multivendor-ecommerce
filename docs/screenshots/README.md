# Screenshots

Save the three screenshots here with these **exact** filenames (PNG), so the links
in the main [README](../../README.md) resolve:

| Filename                 | What it shows                                            |
| ------------------------ | ------------------------------------------------------- |
| `storefront-home.png`    | Storefront home — hero, categories, featured products    |
| `vendor-dashboard.png`   | Vendor dashboard — KPI cards + six-month sales chart     |
| `order-tracking.png`     | Customer order detail — animated tracking timeline       |

Tip: PNG keeps text crisp. Once added, you can delete this note file if you like.
